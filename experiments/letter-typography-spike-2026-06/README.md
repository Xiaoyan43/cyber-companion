# Letter Typography Spike (2026-06)

新的信笺打字机 UI spike，独立单文件 HTML（无依赖、无 Canvas/WebGL），直接在浏览器打开 `index.html` 即可。

来源：`~/Documents/Codex/2026-06-13/ui/outputs/her-letter-typography/index.html`（原样拷贝）。

与本目录下其他旧 spike（`typography-emotion-sample/`、`being-spike/`）无关，互不依赖、互不修改。

当前 4 个 mood：`calm` / `hesitant` / `excited` / `fragile`，通过 `setMood(mood)` 切换。

下一步（未做）：mood 映射表草案，见 TASK_QUEUE P1。
