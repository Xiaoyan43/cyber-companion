# Environment
.env
.env.*
!.env.example

# Downloaded credential files — never commit (Volcengine/cloud console dumps)
AccessKey*.txt
AccessKey*.csv
*ak_sk*
*secret*.txt

# Live provider config (account-specific endpoint ids) — example is the template
config/providers.json

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
venv/

# Node
node_modules/
dist/
build/
.next/
.turbo/

# Desktop / Tauri / Electron artifacts
src-tauri/target/
*.dmg
*.app/

# Local runtime data
data/*.db
data/*.sqlite
data/*.sqlite3
data/runtime/
data/backups/
data/pipecat_spike/
data/fish_model_ab/
data/fish_rhythm_ab/
data/tagger_eval/
data/tagger_position_listen/
data/ja_voice_audition/
logs/

# Generated media
exports/
recordings/

# OS
.DS_Store

# Scratch / tooling — never publish (vendor doc scrapes, local agent config)
.firecrawl/
.claude/
.mcp.json
.cursor/mcp.json
.cursor/skills/
.agents/
skills-lock.json

# Reference drop — vendor/API docs the user hands to Claude; never publish (copyright)
reference/

